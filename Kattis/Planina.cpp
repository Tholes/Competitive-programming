#include <bits/stdc++.h>
#define endl '\n'

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int n,mir;
    cin >> n;
    mir = pow(2,n) + 1;
    cout << mir *  mir << endl;
    return 0;
}
